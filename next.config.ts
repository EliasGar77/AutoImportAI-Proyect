import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  // Si tu repositorio se llama "mobile-de-clone", descomenta la siguiente línea y pon el nombre de tu repositorio:
  // basePath: '/mobile-de-clone',
  // assetPrefix: '/mobile-de-clone',
};

export default nextConfig;
